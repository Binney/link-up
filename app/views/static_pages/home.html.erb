<% provide(:site_section, "0") %>
<%= render 'shared/main_search' %>

<% if signed_in? %>

  <% if current_user.admin? %>
    <div id="local-map">
      <%= gmaps4rails(@json) %>
    </div>
    <div id="diary-events">
      <p>Events in your diary go here</p>
    </div>
    <div id="news">
      <p>News feed goes here</p>
    </div>


<!-- Eventually get similarly personalised homepages for organisers and mentors. -->







  <% else %>
    <div id="local-map">
      <%= gmaps4rails(@json) %>
    </div>
    <div id="diary-events">
      <p>Events in your diary go here</p>
    </div>
    <div id="news">
      <p>News feed goes here</p>
    </div>
  <% end %>

<% else %>

  <div id="local-map">
    <%= gmaps4rails(@json) %>
  </div>
  <div id="today-events">
    <%= render 'events/on_today' %>
  </div>
  <div id="popular-tags">
    <%= render 'tags/brief_selection' %>
  </div>

<% end %>
