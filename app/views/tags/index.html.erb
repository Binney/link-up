<% provide(:title, 'All categories') %>
<% provide(:site_section, "0") %>
<h1>Browse categories</h1>

<% if @tags.any? %>

  <ul class="tags">
    <%= render @tags, width: 150, withnames: true %>
  </ul>

<% else %>

  <p>No tags listed.</p>

<% end %>
