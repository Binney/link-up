<div id="tag_selection">
  <h3>Popular Near You:</h3>
  <% if @tags.any? %>
    <% @tags.each do |tag| %>
      <%= link_to tag.name, tag %> / 
    <% end %>
  <% else %>
    No tags found.
  <% end %>
</div>
