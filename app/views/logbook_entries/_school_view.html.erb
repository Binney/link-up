<% if users.any? %>

  <% users.each do |user| %>
  	<%= render(partial: 'users/logbook_preview', locals: {user: user, templates: templates}) %>
  <% end %>

<% else %>
	
  No users found.

<% end %>
