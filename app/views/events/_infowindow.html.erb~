<style>
  .squirtle {
      text-align: left;
      background-color: #f7931e;
      color: #00ff00;
    h1, p {
      color: #ff0000;
    }
  }
</style>
<div id="squirtle">
  <h1><%= link_to event.name, event %></h1>
  <p>at <%= link_to event.venue.name, event.venue %></p>
  <p><%= event.get_day_info %></p>
  <p><%= event.description %></p>
  <%= render 'events/week_view' %>
</div>
