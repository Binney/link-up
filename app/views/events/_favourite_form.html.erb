<div id="favourite_form">
<% if current_user.favourites.find_by(event_id: @event.id, start_time: oclock) %>
  <%= render 'events/unfavourite', starting: oclock %>
<% else %>
  <%= render 'events/favourite', starting: oclock %>
<% end %>
</div>
