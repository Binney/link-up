<div id="favourite_form">
<% if current_user.favourites.find_by(event_id: @event.id, day: wday) %>
  <%= render 'events/unfavourite', weekday: wday %>
<% else %>
  <%= render 'events/favourite', weekday: wday %>
<% end %>
</div>
