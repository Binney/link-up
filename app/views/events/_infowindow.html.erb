<style>
  .squirtle {
      text-align: left;
      background-color: #f7931e;
      color: #00ff00;
    h1, p {
      color: #ff0000;
    }
  }
</style>
<div id="squirtle">
  <h1><%= event.name %></h1>
  <p>at <%= event.venue.name %></p>
  <% arr = []
  event.timings.each do |t|
    arr.push(Date::DAYNAMES[t.start_time.wday]+"s")
  end %>
  <p><%= arr.nil? ? "None listed" : arr.to_sentence %></p>
  <p><%= event.description %></p>
  <%= link_to "JOIN ACTIVITY", '#' %>
</div>
