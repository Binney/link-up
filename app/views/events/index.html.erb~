<% provide(:title, 'All events') %>
<% provide(:site_section, 0) %>
<h1>All events</h1>

<aside class="events_map">
  <%= gmaps4rails(@json) %>
</aside>
<div id="events_list">
  <div id="search-toggle">
    <h4 id="toggleform">Search</h4>
  </div>
  <div class="neweventform">
    <%= render 'search' %>
  </div>

  <% if @events.any? %>

    <ul class="generic-list">
      <%= render @events, size: 52 %>
    </ul>
    <%= will_paginate @events %>

  <% else %>

    <p>No events found.</p>

  <% end %>
</div>
