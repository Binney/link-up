<%= form_for(current_user.favourites.build(event_id: @event.id, day: weekday), remote: true) do |f| %>
  <div><%= f.hidden_field :event_id %>
  <%= f.hidden_field :day %></div>
  <%= f.submit "Favourite", class: "btn btn-primary" %>
<% end %>
