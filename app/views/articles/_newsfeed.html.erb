<div class="generic-list">
  <h3>News</h3>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article %> (<%= article.created_at.to_s(:short) %>)</li>
  <% end %>
  <%= link_to "More", news_path %>
</div>
